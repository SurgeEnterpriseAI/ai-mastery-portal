node_modules
.next
out
.env.local
.env
data/db.json
npm-debug.log*
.DS_Store

# local prisma sqlite (verification only)
prisma/dev.db
*.db
*.db-journal
.env

# committed DB dump removed — never track local data store (contains password hashes)
data/
